const Index = () => {
  return <div style={{
    display: "flex",
    flexDirection: "column"
  }}>
    <a href="/client">Client rendered page</a>
    <a href="/server">Server rendered page</a>
  </div>
}

export default Index;